/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cary.net', 'loremflickr.com', 'dangelo.org', 'mac.name', 'grayce.org', 'jarred.com', 'devyn.biz', 'annetta.biz'], // Agrega el dominio de tu imagen aquí
  },
};

module.exports = nextConfig;
